# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
addons:
  apt:
    packages:
      - libx11-dev
      - mesa-common-dev
      - libglu1-mesa-dev
